# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android / IDE
.idea/
*.iml
local.properties
captures/
.externalNativeBuild/
.cxx/

# OS
.DS_Store

# Planning / scratch — keep out of the repo
docs/planning/
*.local.md

# TcpRelayMain's persisted node id — machine-local, one per laptop that runs the relay
.relay-node-id*

# Emulation / demo scratch and the Vercel build output
.emulation/
.dashboard-demo/
web/dist/
.vercel
